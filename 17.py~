from num2words import num2words

total = 0

for x in range(1, 1001):
  words = num2words(x)
  words = words.replace(" ", "")
  words = words.replace("-", "")
  words = words.replace(",", "")
  total += len(words)

print total
